# roadmap v1

## phase 0 — install dep + bump version

**prereqs**: none

**read before**:
- `.behavior/v2026_02_07.noble/3.3.blueprint.v1.i1.md` (blueprint)

**checklist**:
- [ ] install `@noble/hashes` at exact version `2.0.1`
- [ ] bump `package.json` version to `3.0.0`
- [ ] run `pnpm install --frozen-lockfile` (or regenerate lockfile if needed)

**verify**:
- [ ] `pnpm list @noble/hashes` shows `2.0.1`
- [ ] `package.json` version is `3.0.0`

---

## phase 1 — delete md5

**prereqs**: phase 0

**read before**:
- `.behavior/v2026_02_07.noble/1.vision.md` (md5 dropped rationale)

**checklist**:
- [ ] delete `src/domain.operations/casts/asHashMd5.ts`
- [ ] delete `src/domain.operations/casts/asHashMd5.test.ts`
- [ ] remove `export { asHashMd5 }` from `src/index.ts`

**verify**:
- [ ] `npm run test:types` passes (no dangled references to md5)

---

## phase 2 — swap asHashSha256 to noble + sync

**prereqs**: phase 1

**read before**:
- `.behavior/v2026_02_07.noble/3.3.blueprint.v1.i1.md` (codepath for asHashSha256)
- `.behavior/v2026_02_07.noble/2.1.criteria.blackbox.md` (usecase.1 — sha256 criteria)

**checklist**:
- [ ] rewrite `src/domain.operations/casts/asHashSha256.ts`:
  - replace `import * as crypto from 'crypto'` with `import { sha256 } from '@noble/hashes/sha2'` and `import { bytesToHex } from '@noble/hashes/utils'`
  - change signature from `async (message: string): Promise<Hash>` to `(message: string): Hash`
  - body: `TextEncoder.encode` → `sha256` → `bytesToHex` → cast as `Hash`
- [ ] update `src/domain.operations/casts/asHashSha256.test.ts`:
  - remove all `await` / `async` from test calls
  - add test vector: `'hello world'` → `b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9`
  - add test vector: `''` → `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
  - add unicode test: `'你好世界'` → valid 64-char hex
  - add determinism test: same input → same output
  - add uniqueness test: different inputs → different outputs

**verify**:
- [ ] `npm run test:unit -- asHashSha256` passes
- [ ] `npm run test:types` passes

---

## phase 3 — delete asHashSha256Sync

**prereqs**: phase 2

**checklist**:
- [ ] delete `src/domain.operations/casts/asHashSha256Sync.ts`
- [ ] remove `export { asHashSha256Sync }` from `src/index.ts`
- [ ] update `src/domain.operations/checks/isHashSha256.test.ts`:
  - replace `import { asHashSha256Sync } from '@src/domain.operations/..'` with `import { asHashSha256 } from '../casts/asHashSha256'`
  - replace `asHashSha256Sync(...)` calls with `asHashSha256(...)`

**verify**:
- [ ] `npm run test:types` passes (no dangled references)
- [ ] `npm run test:unit -- isHashSha256` passes

---

## phase 4 — swap asHashShake256 to noble + sync

**prereqs**: phase 2

**read before**:
- `.behavior/v2026_02_07.noble/3.3.blueprint.v1.i1.md` (codepath for asHashShake256)
- `.behavior/v2026_02_07.noble/2.1.criteria.blackbox.md` (usecase.2 — shake256 criteria)

**checklist**:
- [ ] rewrite `src/domain.operations/casts/asHashShake256.ts`:
  - replace `import * as crypto from 'crypto'` with `import { shake256 } from '@noble/hashes/sha3'` and `import { bytesToHex } from '@noble/hashes/utils'`
  - change signature from `async (...): Promise<Hash>` to `(...): Hash`
  - body: `TextEncoder.encode` → `shake256(msgBytes, { dkLen: options.bytes })` → `bytesToHex` → cast as `Hash`
- [ ] update `src/domain.operations/casts/asHashShake256.test.ts`:
  - remove all `await` / `async` from test calls
  - retain: default 32 bytes → 64-char hex test
  - retain: custom 16 bytes → 32-char hex test
  - retain: custom 64 bytes → 128-char hex test
  - retain: uniqueness test

**verify**:
- [ ] `npm run test:unit -- asHashShake256` passes
- [ ] `npm run test:types` passes

---

## phase 5 — full suite verification

**prereqs**: phases 1–4

**read before**:
- `.behavior/v2026_02_07.noble/2.1.criteria.blackbox.md` (all usecases)

**checklist**:
- [ ] `npm run test:types` — all types pass, no `crypto` references remain
- [ ] `npm run test:format` — format clean
- [ ] `npm run test:lint` — lint clean (depcheck confirms no unused deps, `crypto` not referenced)
- [ ] `THOROUGH=true npm run test:unit` — all unit tests pass
- [ ] `npm run build` — compiles clean
- [ ] `npm run test:acceptance:locally` — acceptance tests pass (if any)
- [ ] verify `src/index.ts` exports exactly: `Hash`, `asHashSha256`, `asHashShake256`, `isHashSha256`
- [ ] verify no `import` of `crypto` remains anywhere in `src/`
